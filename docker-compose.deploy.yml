services:
  predictall:
    build: .
    image: predictall:local
    container_name: predictall
    env_file:
      - .env
    ports:
      - "3010:3000"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
